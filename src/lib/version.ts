/**
 * Current in-flight GAAP Cloud baseline (single in-app source of truth).
 *
 * Bump only at release time, per the release flow in the repo-root
 * `VERSIONING.md`: set the released baseline there, mark it `released` in
 * `src/lib/changelog.ts`, and mirror the notes in the root `CHANGELOG.md`.
 */
export const APP_VERSION = 'v0.0.2-beta';
